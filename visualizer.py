from config import Config


class Visualizer:
    """A class to visualize varius aspects of a model."""

    def __init__(self, model, optimizer, criterion, cfg: Config):

    def plot_training_loss(self, losses):

    def plot_validation_metrics(self, metrics):
        """Plot validation metrics over epochs."""

    def plot_graph(self, graph_data):
        """Visualize the graph structure."""